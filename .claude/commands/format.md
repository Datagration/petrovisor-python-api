Format the petrovisor package source with ruff, then verify no lint errors remain.

```bash
uvx ruff format petrovisor/
uvx ruff check petrovisor/
```

Report which files were reformatted and confirm lint is clean.
