# Development dependencies for petrovisor python api project
# Note: Package installation dependencies are defined in pyproject.toml

# Testing
pytest
pytest-cov

# Documentation
pydoc-markdown==4.6.4
docstring-parser==0.15

# Development tools
pre-commit
ruff
hatchling
build
uv

# The dependencies below are duplicated from pyproject.toml for development environment setup
# You can install them with: pip install -e .
# or: uv pip install -e .

# Core dependencies (for reference, defined in pyproject.toml)
# requests
# pydantic
# pandas
# numpy
# openpyxl
# xlsxwriter
# typing-extensions