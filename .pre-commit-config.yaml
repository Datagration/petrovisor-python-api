repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.7.0
    hooks:
      # Run the linter/checker with auto-fix
      - id: ruff
        args: [--fix]
      # Run the formatter
      - id: ruff-format